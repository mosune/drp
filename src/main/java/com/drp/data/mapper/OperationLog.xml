<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.drp.data.dao.impl.OperationLogDaoImpl">

	<sql id="table"> operation_log </sql>
	
	<resultMap type="operationLog" id="operationLogMap">
		<result property="id" column="id"/>
		<result property="adminUserId" column="admin_user_id"/>
		<result property="descCode" column="desc_code"/>
		<result property="shopId" column="shop_id"/>
		<result property="createBy" column="create_by"/>
		<result property="createTime" column="create_time"/>
		<result property="updateBy" column="update_by"/>
		<result property="updateTime" column="update_time"/>
	</resultMap>
	
	<sql id="sql_where">
		<if test="id != null"> and id = #{id} </if>
		<if test="admin_user_id != null and admin_user_id != ''"> and admin_user_id = #{admin_user_id} </if>
		<if test="desc_code != null and desc_code != ''"> and desc_code = #{desc_code} </if>
		<if test="shop_id != null"> and shop_id = #{shop_id} </if>
		<if test="create_by != null and create_by != ''"> and create_by = #{create_by} </if>
		<if test="create_time != null"> and create_time = #{create_time} </if>
		<if test="update_by != null and update_by != ''"> and update_by = #{update_by} </if>
		<if test="update_time != null"> and update_time = #{update_time} </if>
	</sql>
	
	<sql id="sql_where_key">
		id = #{id}
	</sql>
	
	<sql id="sql_order_key">
		id
	</sql>
	
	<insert id="insert" parameterType="operationLog" >
		insert into
		<include refid="table" />
		(
			id, admin_user_id, desc_code, shop_id, create_by, create_time, update_by, update_time
		) values(
			#{id}, #{adminUserId}, #{descCode}, #{shopId}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}
		)
	</insert>
	
	<insert id="batchInsert" parameterType="java.util.List" >
		insert into
		<include refid="table" />
		(
			id, admin_user_id, desc_code, shop_id, create_by, create_time, update_by, update_time
		) values
		<foreach collection="list" item="item" index="index" separator="," >
			(#{item.id}, #{item.adminUserId}, #{item.descCode}, #{item.shopId}, #{item.createBy}, #{item.createTime}, #{item.updateBy}, #{item.updateTime})
		</foreach>
	</insert>
	
	<delete id="delete" parameterType="operationLog">
		delete from
		<include refid="table" />
		<where>
			<include refid="sql_where_key" />
		</where>
	</delete>
	
	<update id="update" parameterType="operationLog">
		update
		<include refid="table" />
		<set>
			id = #{id},
			admin_user_id = #{adminUserId},
			desc_code = #{descCode},
			shop_id = #{shopId},
			create_by = #{createBy},
			create_time = #{createTime},
			update_by = #{updateBy},
			update_time = #{updateTime}
		</set>
		<where>
			<include refid="sql_where_key" />
		</where>
	</update>
	
	<!-- 按主键查询 -->
	<select id="select" parameterType="operationLog" resultMap="operationLogMap">
		select * from
		<include refid="table" />
		<where>
			<include refid="sql_where_key" />
		</where>
	</select>

	<!-- 按参数查询 -->
	<select id="selectBy" parameterType="java.util.Map" resultMap="operationLogMap">
		select * from
		<include refid="table" />
		<where>
			<include refid="sql_where" />
		</where>
	</select>

	<!-- 按参数查询（分页） -->
	<select id="selectPage" parameterType="java.util.Map" resultMap="operationLogMap">
		select * from
		<include refid="table" />
		<where>
			<include refid="sql_where" />
		</where>
		order by
		<include refid="sql_order_key" />
	</select>
	
</mapper>